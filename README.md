# 小云智服 XiaoYun — 服饰电商 AI 售后客服协同工作台

> 面向中小服饰电商的 AI 售后客服系统：懂退换货政策、能溯源回答、低置信度自动转人工、知识缺口自动回流。

## 项目背景

分析 8 份真实服饰电商客服对话后发现：中小店铺的平台机器人答非所问，人工回复慢且不专业，而服饰类目退换货率高达 20-30%，售后咨询量是其他类目的 3-5 倍。

## 核心功能

- **智能问答**：基于 RAG 的售后政策问答，支持多轮对话
- **来源溯源**：每个回答标注来自哪份政策文档
- **置信度评分**：低置信度回答自动标记，坐席确认后发送
- **知识缺口闭环**：无答案问题 → 转人工 + 记录缺口 → 运营补充标准答案 → 自动回流知识库 → 同类问题下次可被检索
- **知识库管理**：支持上传/管理售后政策、物流说明、FAQ
- **会话与工单**：对话历史、人工接管、工单流转
- **效果看板**：解决率、AI 采纳率、工单分布等核心指标（见下方「在线效果看板」）

## 技术栈

- **前端**：React + Vite + Tailwind CSS（设计系统：绿调 token 体系 + `.ds-*` 组件基类 + AI 协作硬约束）
- **后端**：Python FastAPI + SQLAlchemy
- **数据库**：SQLite（演示）；生产化规划 PostgreSQL + 多租户
- **AI**：智谱 GLM-4-Flash + FAISS 向量检索（RAG）
- **认证**：JWT

## 快速启动

### 1. 后端

```bash
cd backend
pip install -r requirements.txt
cp .env.example .env        # Windows: copy .env.example .env
uvicorn app.main:app --reload --port 8000
```

API 文档：http://localhost:8000/docs

### 2. 前端

```bash
cd frontend
npm install
cp .env.example .env        # Windows: copy .env.example .env
npm run dev
```

访问：http://localhost:5173

### 3. 演示数据（首次启动自动灌入）

后端首次启动且数据库为空时，会自动创建演示租户与样例数据。

也可手动重置演示数据：

```bash
cd backend
python scripts/seed_data.py
```

**演示账号**：`testadmin` / `123456`（部署环境可通过 `XIAOYUN_DEMO_PASSWORD` 修改）  
**买家端**：http://localhost:5173/chat  
**坐席工作台**：http://localhost:5173/login → 会话管理

> 买家端对话需配置 `backend/.env` 中的 `LLM_API_KEY`（智谱 API Key），否则 AI 问答会走拒答/转人工。

### 演示路径建议

1. 买家端 `/chat` 提问「尺码不合适能换货吗？」→ 查看 AI 回答与来源
2. 提问无关问题或点击「转人工」→ 会话进入待人工队列
3. 坐席登录 → 会话管理 → 筛选「待人工」→ 采纳建议或手动回复
4. 买家端轮询可见坐席回复；坐席可「转工单」到工单中心

在线 Demo：http://47.116.143.103:8080 （教学部署，可能随服务器到期下线；正式演示以本 README + 录屏为准）

## 在线效果看板（作品集演示组件）

访问 **http://47.116.143.103:8080/eval.html** 可直接查看实时「RAG 评测 + 运营看板」：

- RAG 评测：评测题数、检索命中率、平均置信度、高置信率、badcase 数、逐题明细
- 运营看板：总会话数、消息数、AI 采纳率、知识库文档（已索引/总数）、工单分布
- 后端接口：`POST /api/eval/run`、`GET /api/stats/dashboard`

## 设计系统（Design System）

为避免「vibe coding 每次改 UI 风格漂移」，本项目落地了一套可被 AI 调用的设计系统（方法论参考小红书「机器坏人 AI 版」《如何用 AI 维护一套设计系统》）：

- **品牌色系统化**：原有品牌绿 `#1D9E75` 扩展为 50–900 完整色阶，并定义 success / warning / danger / info 语义色，收敛原先散落的 blue/indigo/orange/green/red。
- **组件基类约束**：`src/index.css` 的 `@layer components` 定义 `.ds-card` / `.ds-btn-*` / `.ds-badge-*` / `.ds-input` / `.ds-stat-*` 等基类；页面只能复用，新增组件先扩展此处。
- **AI 协作硬约束**：`.cursorrules` 写死——颜色只能来自 token、布局只能复用 `.ds-*`、禁止硬编码与新增主色。让 AI 改界面从「随心所欲」变为「在约束下选择」。
- **反哺机制**：重复结构抽组件、好风格改 token 一处联动全站。

在线查看完整 Token 与组件真实渲染：**http://47.116.143.103:8080/design-system.html**

## 评测与质量保障

针对售后场景设计了 **17 题黄金测试集**，对线上 Demo 自动跑评测，覆盖退换货 / 物流 / 尺码 / 支付 / 售后政策五大类真实售后场景。

| 指标 | 结果 |
|---|---|
| 总题数 | 17 |
| 检索命中率 | **100%**（17/17） |
| 平均置信度 | **0.68** |
| 高置信率（≥0.7） | 29.4% |
| Badcase 数 | **0** |
| 知识库 | **18 篇**带边界感的政策文档（含「内衣不支持无理由退」「吊牌剪但质量问题可退」「改地址」「赠品退回」等冲突/缺口场景），召回率 100% |

**置信度阈值校准**：将 `confidence_threshold` 从 0.4 校准到 0.6，使低置信问题能正确触发人工兜底与知识缺口沉淀（原 0.4 低于实际最低置信度 0.5，安全网形同虚设）。

**产品闭环：知识缺口自动回流**
问「卖不卖宠物狗粮」(conf 0.58 < 0.6) → 自动转人工 + 记录知识缺口 → 后台标记已解决 → **自动回流知识库（13 → 14 篇）** → 再问可正确回答（conf 0.64）。证明系统具备「用户问倒 → 人工补知识 → 模型变聪明」的自迭代闭环，而非静态问答。

## 数据来源与边界（诚实说明）

- 知识库（18 篇政策文档）基于**服饰电商售后领域公开语料 + 真实客服对话模式提炼**构建，覆盖退换货 / 物流 / 尺码 / 支付 / 质量 / 会员 / 优惠 / 大促 / 色差洗护 / 定制预售 / 地址修改 / 赠品退回等场景，并包含多项冲突边界案例。
- 评测集为人工编写的 **17 题黄金集**，用于量化 RAG 检索效果，不代表线上全量分布。
- 演示客户「云裳服饰」为虚构示例；全部数据为**演示用途，非真实生产店铺数据**。这一边界在面试中主动说明，反而体现对数据真实性与评测局限性的产品判断。

## 项目结构

```
xiaoyun/
├── docs/              # 产品文档（定位/需求/竞品/Cursor开发PRD）
├── prototype/         # HTML 原型（25页）
├── backend/           # FastAPI 后端
│   ├── app/
│   │   ├── core/      # 配置与安全
│   │   ├── models/    # 数据库模型
│   │   ├── routers/   # API 路由（含 stats / eval）
│   │   ├── services/  # RAG / LLM / AI 建议 / 评测
│   │   └── main.py
│   └── requirements.txt
├── eval/              # 17 题黄金评测集
└── frontend/          # React 前端
    └── src/
```

## 文档索引

| 文档 | 说明 |
|------|------|
| [产品定位](docs/00_产品定位.md) | 一句话定位、目标用户、产品边界、差异化 |
| [需求分析与用户研究](docs/01_需求分析与用户研究.md) | 痛点诊断、用户画像、KPI 体系 |
| [竞品分析与差异化定位](docs/02_竞品分析与差异化定位.md) | 竞品拆解、差异化支柱 |
| [Cursor开发文档PRD](docs/03_Cursor开发文档PRD.md) | 技术栈、数据表、API、开发计划 |

## 开发进度

- [x] 阶段0：产品定位
- [x] 阶段1：需求分析与用户研究
- [x] 阶段2：竞品分析
- [x] 阶段3：产品设计（PRD + 25页 HTML 原型）
- [x] 阶段4：技术实现（阶段 A-D 全部完成）
- [x] 阶段5：评测与效果验证（17 题自动评测，检索 100%，0 badcase）
- [x] 阶段6：包装与求职（效果看板上线 + 评测数据入 README）

## 作品集说明

本项目为 AI 产品经理求职作品集，每份文档内嵌「面试话术」模块，覆盖端到端链路设计、评测方法、badcase 迭代等。
